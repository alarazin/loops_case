# This file is an example. Create a real .env file and fill in your details.
# Do not commit the .env file to version control.

GCP_PROJECT_ID="your-gcp-project-id"
GCS_BUCKET_NAME="your-unique-gcs-bucket-name"
